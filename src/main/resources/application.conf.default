ktor {
    deployment {
        port = 80
        sslPort = 443
    }
    application {
        modules = [ minepop.exhibit.ExhibitServerKt.module ]
    }
    security {
        ssl {
            keyStore = /path/to/keystore
            keyAlias = alias
            keyStorePassword = changeit
            privateKeyPassword = changeit
        }
    }
}